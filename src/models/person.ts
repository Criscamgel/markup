import { Birthday } from './birthday';

export class Person {
    nombre: string;
      tipoIdetificacion: string;
      numeroIdentificacion: number;
      email: string;
      celular: number;
      fechaNacimiento: Birthday;
      actividadEco: string;
      ingresos: number;
}
